'use strict';

{
  const product = 'Vodka';
  const productCount = 12;
  const productPrice = 500;

  console.log('product: ', product);

  // ✔ В консоль выведите общую сумму товара
  const totalAmount = productCount * productPrice;
  console.log('Вбуханно в водку: ', `${totalAmount} RUB`);
}

// New Product
{
  const product = 'Opel';
  console.log('product: ', product);
}
